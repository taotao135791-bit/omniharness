# OpenClaw Upstream Audit

Source-level audit of OpenClaw for OmniHarness integration planning — specifically to reuse its **gateway / channel / node** capabilities as an adapter, **not** its agent loop.
Audit date: 2026-07-23. Audited from a `git clone --depth 1` into `tmp/upstream/openclaw` (gitignored, not committed). All paths below are relative to `tmp/upstream/openclaw/`.

## 1. Identity & version audited

- **Repo audited:** https://github.com/openclaw/openclaw — it **exists**. It is Peter Steinberger's personal-AI-assistant gateway, formerly known as **Clawdbot**, then **Moltbot**, now OpenClaw (built for "Molty", a space lobster assistant). It is the project the task meant; no fallback search target was needed.
- **Commit:** `cca67fc842409367ef649ecf63828b412242ccb4` (2026-07-23, "fix(meetings): verify talk-back loopback signal (#112939)")
- **Version:** `2026.7.2` (`package.json`; calendar versioning, all workspace packages version-locked). State schema versions: `state: 5`, `agent: 13`.
- **License:** **MIT** (`LICENSE`, "Copyright (c) 2026 OpenClaw Foundation"). `THIRD_PARTY_NOTICES.md` is minimal (37 lines, one entry: Pi / pi-mono, MIT, © 2025 Mario Zechner — "Portions of OpenClaw were adapted from Pi / pi-mono", plus runtime dep `@earendil-works/pi-tui`). It is hand-maintained, not generated — if we vendor/bundle OpenClaw `dist/` output we inherit its bundled-dependency notice gap (zod etc. are bundled by tsdown without per-dep notices).
- **Scale/shape:** pnpm monorepo, ~28k files. Core in `src/` (142 MB incl. tests), ~200 bundled plugin packages in `extensions/`, shared libraries in `packages/`, native apps in `apps/{ios,android,macos,linux,windows-hub}`, docs in `docs/`. Node ≥22.22.3 / 24.15 / 25.9 required (`openclaw.mjs` launcher hard-gates; Bun rejected at runtime because it needs `node:sqlite`).

## 2. Architecture map (real paths)

### Gateway server (`src/gateway/`, `packages/gateway-protocol/`, `packages/gateway-client/`)

- **One multiplexed TCP port, default `18789`**, serving HTTP + WebSocket on the same listener. No Unix socket.
  - `src/gateway/server.impl.ts:600` — `startGatewayServer(port = 18789, opts)` (public programmatic API; also called by the setup wizard `src/wizard/setup.finalize.ts:95`).
  - `src/gateway/server-runtime-state.ts:285-354` — wiring: one `WebSocketServer({noServer: true, maxPayload: MAX_PREAUTH_PAYLOAD_BYTES})`, per-bind-host `http.Server` via `src/gateway/server-http.ts` (`createGatewayHttpServer`, `attachGatewayUpgradeHandler` at :960), optional TLS (`gateway.tls`: `enabled`, `autoGenerate`, `certPath/keyPath/caPath`), optional second loopback-only "worker ingress" server.
  - `src/gateway/server-constants.ts` — `MAX_PREAUTH_PAYLOAD_BYTES` = 64 KiB, post-handshake `MAX_PAYLOAD_BYTES` = 25 MiB, `MAX_BUFFERED_BYTES` = 50 MiB per connection.
  - Bind policy `GatewayBindMode = "auto"|"lan"|"loopback"|"custom"|"tailnet"` (`src/config/types.gateway.ts:5`), default loopback; non-loopback logs a loud warning. Discovery: mDNS/Bonjour (`gateway.discovery.mdns`), Tailscale serve/funnel (`src/gateway/server-tailscale.ts`).
  - HTTP surface on the same port: `/health(z)`, `/ready(z)`, Control UI static, `POST /v1/chat/completions`, `POST /v1/responses`, `/v1/models`, `/v1/embeddings`, `POST /tools/invoke`, `/sessions/:key/history|/kill`, `/hooks/*`, plugin routes (`src/gateway/server-http.ts:138-230`).

- **Wire protocol** (`packages/gateway-protocol/`): WS text frames, JSON, TypeBox schemas.
  - `src/schema/frames.ts` — `GatewayFrameSchema` union on `"type"`: `RequestFrame {type:"req", id, method, params?}`, `ResponseFrame {type:"res", id, ok, payload?, error?}`, `EventFrame {type:"event", event, payload?, seq?, stateVersion?}`; `ErrorShape {code, message, details?, retryable?, retryAfterMs?}` (codes in `src/schema/error-codes.ts`: `INVALID_REQUEST`, `NOT_PAIRED`, `UNAVAILABLE`, `FORBIDDEN`, …).
  - `src/version.ts` — `PROTOCOL_VERSION = 4`, `MIN_CLIENT_PROTOCOL_VERSION = 4`, `MIN_NODE_PROTOCOL_VERSION = 3`.
  - Kotlin/Swift client constants are **generated from these schemas** (`scripts/protocol-gen-kotlin.ts`).

- **Handshake** (`src/gateway/server/ws-connection.ts` + `ws-connection/message-handler.ts`):
  1. On connect, server sends `{type:"event", event:"connect.challenge", payload:{nonce, ts}}` (randomUUID nonce, handshake timeout).
  2. First client frame **must** be `{type:"req", method:"connect", params: ConnectParams}` (else close 1008). `ConnectParamsSchema` (`frames.ts:32`): `minProtocol`, `maxProtocol`, `client{id, displayName?, version, platform, deviceFamily?, modelIdentifier?, mode, instanceId?}` (closed enums in `packages/gateway-protocol/src/client-info.ts`), `caps?`, `commands?`, `permissions?`, `role?`, `scopes?`, `device?{id, publicKey, signature, signedAt, nonce}`, `auth?{token?, bootstrapToken?, deviceToken?, password?, approvalRuntimeToken?, agentRuntimeIdentityToken?}`, `locale?`, `userAgent?`.
  3. Pipeline: `admitGatewayConnect` (`connect-admission.ts:55`) → `authenticateGatewayConnect` (`connect-auth.ts:48`) → `verifyGatewayConnectDeviceProof` (`connect-device-proof.ts:16`) → `authorizeGatewayConnectDevice` (`connect-device-pairing.ts:78`) → `attachAuthenticatedGatewayConnect` (`connect-session.ts:83`) → `sendGatewayHello` (`connect-hello.ts:32`).
  4. Success payload `HelloOkSchema` (`frames.ts:75`): `{type:"hello-ok", protocol, server{version, connId}, features{methods[], events[], capabilities?}, snapshot, auth{deviceToken?, role, scopes[], issuedAtMs?, deviceTokens?[]}, policy{maxPayload, maxBufferedBytes, tickIntervalMs}, ...}`.

- **Auth layers:**
  - Shared secret: `gateway.auth.mode = "none"|"token"|"password"|"trusted-proxy"` (`src/config/types.gateway.ts:217`; default `token`; env `OPENCLAW_GATEWAY_TOKEN`/`_PASSWORD`). Resolution `resolveGatewayAuth()` (`src/gateway/auth-resolve.ts:34`); constant-time compare `safeEqualSecret` (`src/security/secret-equal.ts`); per-IP sliding-window rate limit (`src/gateway/auth-rate-limit.ts:140`, 10 attempts/60s → 5 min lockout, loopback exempt); known-weak-secret check `src/gateway/known-weak-gateway-secrets.ts`. Tailscale mode verifies `tailscale-user-login` against local `tailscale whois`; trusted-proxy requires peer IP in `gateway.trustedProxies` + `userHeader`. Browser Origin checks `src/gateway/origin-check.ts`.
  - Device proof (second factor): per-device **Ed25519** keypair (`src/infra/device-identity.ts`: `loadOrCreateDeviceIdentity`, `deriveDeviceIdFromPublicKey` = sha256 hex of pubkey). Client signs `buildDeviceAuthPayloadV3` (`packages/gateway-client/src/device-auth.ts:45`): pipe-joined `"v3"|deviceId|clientId|clientMode|role|scopes|signedAtMs|token|nonce|platform|deviceFamily` — note the **shared token is inside the signed payload**, binding bearer secret to device key. Server verifies deviceId match, ±2 min skew (`DEVICE_SIGNATURE_SKEW_MS`), nonce echo.
  - Device tokens: after pairing, server issues per-role `DeviceAuthToken` (`ensureDeviceToken`, `src/infra/device-pairing.ts:1674`), returned in `hello-ok.auth.deviceToken`, reused on reconnect (`verifyDeviceToken`, :1606; checks pairing record, revocation, issuer generation — rotation of the gateway secret invalidates with close code 4001 "gateway auth changed"). RPCs `device.token.rotate` / `device.token.revoke`.

- **Roles & scopes:** roles `operator` (control plane), `node` (capability host), `worker` (closed cloud protocol). Operator scopes are a closed set (`src/gateway/operator-scopes.ts`): `operator.read|write|admin|approvals|pairing|talk.secrets`. Role→method gating: `isRoleAuthorizedForMethod` (`src/gateway/role-policy.ts:24`) + `authorizeGatewayMethod` (`src/gateway/server-methods.ts:313`). Unpaired shared-auth connects get self-declared scopes **cleared** (`clearUnboundScopes`, `connect-auth.ts:284`).

- **Events:** `createGatewayBroadcaster` (`src/gateway/server-broadcast.ts:153`); every broadcast is an `EventFrame` with per-client monotonic `seq`; scope gating `EVENT_SCOPE_GUARDS` (:33 — chat/agent/session → `operator.read`, approvals → `operator.approvals`, pairing → `operator.pairing`, health/tick/presence unrestricted, **unknown families fail closed**; node/worker connections get nothing). Slow consumers closed 1008. Subscription RPCs `sessions.subscribe` / `sessions.messages.subscribe`. Keepalive ping 25 s. Event families: `chat`, `agent`, `session.message|operation|tool|approval`, `sessions.changed`, `presence`, `tick`, `health`, `cron`, `node.pair.requested|resolved`, `node.invoke.request`, `device.pair.requested|resolved`, `exec.approval.requested|resolved`, `config.changed`, `talk.event`, `update.available` (`docs/gateway/protocol.md`).

- **Startup:** CLI `openclaw gateway` → `src/cli/gateway-cli/run.ts` (~:737) → dynamic import of `startGatewayServer`. Bin `openclaw.mjs` is a Node-version-guarded launcher spawning `dist/entry.js`. Daemon install: `src/daemon/` (launchd `ai.openclaw.gateway`, systemd **user** units, Windows schtasks + Startup-folder fallback); program args `["gateway","--port",N]` (`src/daemon/program-args.ts:250`); single-instance lock `src/infra/gateway-lock.ts`.

### Channels (`src/channels/`, `extensions/<channel>/`)

- **Plugin packaging:** each channel is a workspace package `@openclaw/<channel>` under `extensions/<channel>/` with **two** metadata files:
  - `openclaw.plugin.json` — manifest `{id, icon, activation:{onStartup}, channels:[...], configSchema}`.
  - `package.json` `openclaw` key — real contract: `openclaw.extensions: ["./index.ts"]`, `openclaw.setupEntry`, `openclaw.channel: {id, configuredState:{env:{anyOf:[...]}}, approvalFlags, label, docsPath, markdownCapable, setup:{fields[]}, commands:{...}}`.
  - `extensions/<channel>/index.ts` default-exports `defineBundledChannelEntry({id, plugin:{specifier, exportName}, secrets:{...}, runtime:{...}, accountInspect, registerFull})` (`src/plugin-sdk/channel-entry-contract.ts`) with lazy `loadChannelPlugin()/loadChannelSecrets()/loadChannelOutbound()`. External plugins use `defineChannelPluginEntry` (`src/plugin-sdk/core.ts:566`) → `api.registerChannel({plugin})` (`src/plugins/plugin-api.types.ts:209`), gated by `api.registrationMode` (`"cli-metadata"|"tool-discovery"|"discovery"|"full"`).

- **The `ChannelPlugin` interface** — `src/channels/plugins/types.plugin.ts:60`, `ChannelPlugin<ResolvedAccount, Probe, Audit>`:
  `{id, meta, capabilities, defaults?, reload?, setupWizard?, config: ChannelConfigAdapter, configSchema?, pairing?, security?, groups?, mentions?, outbound?: ChannelOutboundAdapter, status?, gatewayMethods?: string[], gateway?: ChannelGatewayAdapter, auth?, approvalCapability?, commands?, lifecycle?, secrets?, allowlist?, doctor?, bindings?, streaming?, threading?, message?, messaging?, agentPrompt?, directory?, resolver?, actions?, heartbeat?, agentTools?}`.
  Key adapters (`src/channels/plugins/types.adapters.ts`, `types.core.ts`, `outbound.types.ts`, `pairing.types.ts`):
  - `ChannelGatewayAdapter` (:339): `startAccount(ctx: ChannelGatewayContext)`, `stopAccount?`, `loginWithQrStart?/loginWithQrWait?`, `logoutAccount?`. `ChannelGatewayContext` (:241) carries `cfg, accountId, account, runtime, abortSignal, log, getStatus/setStatus` plus host-injected `channelRuntime` (reply/routing/session/media/commands/pairing helpers).
  - `ChannelOutboundAdapter` (`outbound.types.ts:169`): `deliveryMode: "direct"|"gateway"|"hybrid"`, `resolveTarget?`, and send verbs `sendText / sendMedia / sendPayload / sendFormattedText / sendFormattedMedia / sendPoll` — all returning `Promise<OutboundDeliveryResult>`; context `ChannelOutboundContext` (:21) with `to, text, mediaUrl?, threadId?, accountId?, mediaAccess?, replyToId?, ...`.
  - `ChannelSecurityAdapter` (:840): `resolveDmPolicy(ctx) → ChannelSecurityDmPolicy | null`, `collectWarnings`, `collectAuditFindings`.
  - `ChannelPairingAdapter` (`pairing.types.ts:12`), `ChannelAllowlistAdapter` (:667), `ChannelMessageAdapterShape` (`src/channels/message/types.ts:442`: `{send?, live?, receive?, durableFinal?}`), results normalized to `MessageReceipt` (:84).
  - Most chat channels are composed via `createChatChannelPlugin({base, security, pairing, threading, outbound})` (`src/plugin-sdk/core.ts:822`) — confirmed in `extensions/{telegram,slack,discord,whatsapp,signal,imessage}/src/channel.ts`.

- **Per-channel runtime stacks:** telegram = grammy 1.44 (polling `@grammyjs/runner` or webhook), slack = `@slack/bolt` 4.7 Socket Mode (default; HTTP mode needs `signingSecret`), discord = raw `ws` + `discord-api-types`, whatsapp = `baileys@7.0.0-rc13` (persisted auth dir + QR login), signal = `signal-cli` JSON-RPC over WS, imessage = local `imsg` bridge (local IPC trust).

- **Canonical inbound message: `MsgContext`** — `src/auto-reply/templating.ts:94`, a flat ~200-field PascalCase record. Key groups:
  - Body: `Body, BodyForAgent, RawBody, CommandBody, BodyForCommands, CommandArgs, InboundHistory`.
  - Routing/identity: `From, To, SessionKey, AgentId, AccountId, ChatType ("direct"|"group"|"channel"), Provider, Surface, OriginatingChannel, OriginatingTo, ChatId, MessageThreadId, ThreadParentId`.
  - Sender: `SenderId, SenderName, SenderUsername, SenderE164, SenderIsBot`.
  - Media: `MediaPath/MediaUrl/MediaType`, `MediaPaths[]/MediaUrls[]/MediaTypes[]`, `media?: MediaFact[]` ("array position is attachment identity"), `MediaUnderstanding[]`, `Transcript`.
  - **Trust markers:** `CommandAuthorized` (default-deny, set only by `finalizeInboundContext`, `src/auto-reply/reply/inbound-context.ts:60`), `InboundAccessAuthorized` ("Internal proof that the channel ingress owner admitted this sender/event … must fail closed when absent", templating.ts:368), `UntrustedContext[]`, `UntrustedStructuredContext[]`, `OwnerAllowFrom`, `GatewayClientScopes`, `WasMentioned`, `GroupRequireMention`.

- **Ingress access decision:** `resolveChannelMessageIngress` / `createChannelIngressResolver` (`src/channels/message-access/runtime.ts`); inputs `subject`, `conversation:{kind,id}`, `policy:{dmPolicy, groupPolicy}`, `allowFrom`, pairing-store list; output decision + reason codes. Legacy `resolveDmGroupAccessWithLists` (`src/security/dm-policy-shared.ts:233`). `DmPolicy = "pairing"|"allowlist"|"open"|"disabled"` (`src/config/types.base.ts:17`; default `"pairing"` from `buildCommonChannelAccountShape`, `src/config/zod-schema.channel-messaging-common.ts:143`).

- **WebChat is NOT a channel plugin.** It's internal channel id `"webchat"` (`INTERNAL_MESSAGE_CHANNEL`, `src/utils/message-channel-constants.ts:4`). UIs talk gateway WS RPCs `chat.history|chat.send|chat.inject|chat.message.get` (`docs/web/webchat.md`); `chat.send` (scope `operator.write`, `src/gateway/methods/core-descriptors.ts:310`) builds a `MsgContext` with `Provider/Surface:"webchat"`, `ChatType:"direct"` (`src/gateway/server-methods/chat-send-user-turn.ts:150`). No dmPolicy/allowlist — trust is the operator connection itself. Replies return over the same WS broadcast; webchat is rejected as an external outbound target (`src/auto-reply/reply/send.ts:202`).

### Nodes / devices (`src/node-host/`, `src/infra/device-pairing.ts`, `src/infra/node-pairing.ts`, `extensions/device-pair/`)

- **Two-layer trust:** device pairing (transport auth, `src/infra/device-pairing.ts` — 1870 lines) then node pairing (capability gating, `src/infra/node-pairing.ts`; lives inside the device record as `nodeSurface`/`pendingNodeSurface`; requires a paired device first). Nodes are WS clients with `role:"node"`; gateway pushes work as **event** frames, nodes answer with **req** frames. Nodes can call only ~10 node-scope methods (`node.invoke.result|progress`, `node.event`, `node.pending.pull|drain|ack`, `node.pluginTools.update`, `node.skills.update`, `node.pluginSurface.refresh`, `node.presence`); everything else → "unauthorized role".
- **Phone onboarding:** `extensions/device-pair/index.ts` registers `/pair` (scope `operator.pairing`) with `status|pending|notify|approve|cleanup|revoke|qr`. `issueSetupPayload` (:649) → `issueDeviceBootstrapToken` (`src/infra/device-bootstrap.ts`) → `SetupPayload {url, urls?, bootstrapToken, expiresAtMs, access:"full"|"limited"}` base64url-encoded as a setup code / QR PNG. Bootstrap tokens: 32-byte base64url, TTL 10 min (`DEVICE_BOOTSTRAP_TOKEN_TTL_MS`), single-purpose profiles (`src/shared/device-bootstrap-profile.ts`). `validateMobilePairingUrl` (:291) refuses cleartext `ws://` except loopback/private-LAN/`.local`/emulator; plaintext routes downgrade access to `"limited"`.
- **Silent/auto approvals:** loopback local pairing, `gateway.nodes.pairing.autoApproveCidrs`, SSH-verified auto-approve (`src/infra/node-pairing-ssh-verify.ts`, default on — gateway SSHes back and runs `openclaw node identity`), trusted-proxy `deviceAutoApprove`, bootstrap tokens. Approval kinds: `owner|silent|trusted-cidr|trusted-proxy|ssh-verified|bootstrap`.
- **Capabilities** (declared `caps` strings; canonical lists: `apps/android/.../OpenClawProtocolConstants.kt`, policy in `src/gateway/node-command-policy.ts`): `canvas, camera, sms, talk, location, device, notifications, system, photos, contacts, calendar, motion, callLog, voiceWake, mobileUI` (iOS adds `screen, watch, reminders, health`).
- **Command namespaces** (real names): `canvas.present|hide|navigate|eval|snapshot`, `canvas.a2ui.push|pushJSONL|reset`; `camera.list|snap|clip`; `screen.snapshot` (desktop) / `screen.record` (iOS ReplayKit); `computer.act` (desktop input injection); `location.get`; `device.status|info|permissions|health|apps`; `notifications.list|actions`, `system.notify`; `sms.send|search`, `callLog.search`, `contacts.search|add`, `calendar.events|add`, `reminders.list|add`, `photos.latest`, `motion.activity|pedometer`, `health.summary`; `mobile.ui.observe|act` (Android accessibility); `talk.ptt.start|stop|cancel|once`; `watch.status|notify`; node-host surface `system.run.prepare|system.run|system.which`, `system.execApprovals.get|set`, `fs.listDir`, `terminal.upload`, `browser.proxy`, `mcp.tools.call.v1`, `agent.cli.claude.run.v1` (`src/infra/node-commands.ts`).
- **Dangerous commands:** `DEFAULT_DANGEROUS_NODE_COMMANDS` (`node-command-policy.ts:104` — camera snap/clip, screen.record, computer.act, sms.send, mobile.ui.*, health.summary, …) are approvable at pairing but stripped from the runtime allowlist until armed via config `gateway.nodes.commands.allow` / `.deny` (`resolveNodeCommandAllowlistInternal`, :398).
- **Invocation wire:** downlink event `node.invoke.request` (`NodeInvokeRequestEventSchema {id, nodeId, command, paramsJSON?, timeoutMs?, idempotencyKey?}`, `packages/gateway-protocol/src/schema/nodes.ts:228`), `node.invoke.cancel`, `node.invoke.input` (≤16 KB chunks). Uplink req `node.invoke.result {id, nodeId, ok, payload?, payloadJSON?, error?}` / `node.invoke.progress {invokeId, seq, chunk}`. Operator side: RPC `node.invoke` (scope `operator.write`, or `operator.admin` for admin commands — `src/gateway/method-scopes.ts:175`), handler `src/gateway/server-methods/nodes.ts:1566` (blocks `system.execApprovals.*`, enforces runtime allowlist + declared surface; APNs wake for offline iOS nodes). Node-side dispatch: `handleInvoke`/`dispatchInvoke` (`src/node-host/invoke.ts:537-837`).
- **CLI:** `openclaw devices list|approve|reject|remove|rename|rotate|revoke` (`src/cli/devices-cli.ts` → `device.pair.*` RPCs, scope `operator.pairing`); `openclaw nodes pending|approve|reject|remove|invoke|camera|screen|location|status` (`src/cli/nodes-cli/`).

### Message routing & sessions (`src/routing/`, `src/auto-reply/`, `src/sessions/`, `src/channels/`)

- **Pipeline (function names in order):**
  1. Turn kernel `runChannelInboundEvent` (`src/channels/turn/kernel.ts:179/350`) driving `ChannelTurnAdapter<TRaw>` (`src/channels/turn/types.ts:402`): `ingest → classify? → preflight? → resolveTurn → onFinalize?`; admission `ChannelTurnAdmission = dispatch|observeOnly|handled|drop`.
  2. `assembleResolvedChannelTurn` (`src/channels/turn/lifecycle.ts:21`) resolves the per-agent session store and injects `recordInboundSession` (`src/channels/session.ts:29`) + `dispatchReplyWithBufferedBlockDispatcher` (`src/auto-reply/reply/provider-dispatcher.ts:17`).
  3. `recordInboundSession` canonicalizes the key, writes session meta, updates the session's **last route** (`updateSessionLastRoute` with `deliveryContext {channel, to, accountId, threadId}`).
  4. `dispatchInboundMessageWithBufferedDispatcher` (`src/auto-reply/dispatch.ts:574`) → `dispatchInboundMessage` (:513) → `dispatchReplyFromConfig` (`src/auto-reply/reply/dispatch-from-config.ts:225`, ~3000 lines: dedupe, plugin claims, route-reply decision, typing).
  5. `getReplyFromConfig` (`src/auto-reply/reply/get-reply.ts:211`) → `initSessionState` (`src/auto-reply/reply/session.ts:352`) → `handleInlineActions`/`handleCommands` → `runPreparedReply` (`get-reply-run.ts:495`) → `runReplyAgent` (`agent-runner.ts:1172`) → `runAgentTurnWithFallback` (`agent-runner-execution.ts:96`).
- **Session keys** (`src/routing/session-key.ts`, `src/sessions/session-key-utils.ts`):
  - Canonical format **`agent:<agentId>:<rest>`**; `parseAgentSessionKey` (:267). Defaults `DEFAULT_AGENT_ID="main"`, `DEFAULT_MAIN_KEY="main"`, `DEFAULT_ACCOUNT_ID="default"`.
  - Main: `agent:<agentId>:<mainKey>` (`buildAgentMainSessionKey`, :182).
  - Peers (`buildAgentPeerSessionKey`, :191; `dmScope = "main"|"per-peer"|"per-channel-peer"|"per-account-channel-peer"`): group → `agent:<id>:<channel>:<group|channel>:<peerId>`; DM per-channel-peer → `agent:<id>:<channel>:direct:<peerId>`; per-account → `agent:<id>:<channel>:<accountId>:direct:<peerId>`. `session.identityLinks` maps channel peer ids to canonical identities (`resolveLinkedPeerId`, :245).
  - Threads suffix `:thread:<threadId>` (`resolveThreadSessionKeys`, :308); cron `agent:<id>:cron:<job>:run:<runId>`; subagent `subagent:`; ACP `acp:`.
  - Lowercased **except** provider-opaque case-sensitive spans in `CASE_PRESERVING_PEERS` (Signal groups, Matrix room IDs — `session-key-utils.ts:60`). `parseSessionDeliveryRoute` (:397) reverse-parses a key into `{channel, accountId?, peerKind, peerId, threadId}` for outbound delivery.
  - `SessionEntry.sessionId` (UUID) is separate from the key and **rotates on `/new` and stale expiry**; keys are the durable routing identity.
- **Multi-agent routing:** `resolveAgentRoute` (`src/routing/resolve-route.ts:606`) over `cfg.bindings` — `AgentRouteBinding {type?:"route", agentId, match: AgentBindingMatch, session?:{dmScope?}}` with `AgentBindingMatch {channel, accountId?, peer?{kind,id}, guildId?, teamId?, roles?}` (`src/config/types.agents.ts:40-56`). Match tiers: peer → thread-parent → peer wildcard → guild+roles → guild → team → account → channel → default (`resolveDefaultAgentId`). ACP variant: `AgentAcpBinding {type:"acp", agentId, match, acp?:{mode,label,cwd,backend}}` (:68).
- **Reply delivery:** direct path via channel-supplied `ChannelEventDeliveryAdapter {preparePayload?, deliver, durable?, onDelivered?, onError?}` (`src/channels/turn/types.ts:189`); `ReplyDispatcher` (`src/auto-reply/reply/reply-dispatcher.ts:341`) with three lanes: tool / block / final. Cross-channel: `resolveEffectiveReplyRoute` (`effective-reply-route.ts:70`) prefers live ctx, falls back to persisted session route; `routeReply` (`route-reply.ts`) delivers through the target channel's plugin. Typing: `createTypingController` (`typing.ts`), interval `agents.<id>.typingIntervalSeconds ?? 6s`. Streaming modes `off|partial|block|progress` (`src/channels/streaming.ts`). Media normalized by `createReplyMediaPathNormalizer` (`reply-media-paths.runtime.ts`).
- **Commands:** registry `src/auto-reply/commands-registry.shared.ts` (`/new` :790, `/reset` :781, `/status` :257); detection `src/auto-reply/command-detection.ts` (strips metadata via `stripInboundMetadata`, bot-username-aware `normalizeCommandBody`); auth `resolveCommandAuthorization` (`src/auto-reply/command-auth.ts:599`) — owner list from `cfg.commands.ownerAllowFrom` else channel allowFrom fallback, plus `operator.admin` scope on internal channel; `/new`-`/reset` handled in `initSessionState` against `cfg.session.resetTriggers ?? ["/new","/reset"]`, gated by `isResetAuthorizedForContext` (`reset-authorization.ts:7`).
- **Sandbox divergence:** `shouldSandboxSession` (`src/agents/sandbox/runtime-status.ts:23`): `mode "off"` → never, `"all"` → always, `"non-main"` → sandbox unless `sessionKey === mainSessionKey`. **Default is `off`** (`src/agents/sandbox/config.ts:255`). Backends: Docker (default), SSH, OpenShell (`src/agents/sandbox/`).

### Agent invocation (`src/agents/`, `packages/agent-core/`, `packages/acp-core/`, `src/acp/`)

Three seams between the session/router and "the agent":

1. **Embedded in-process runtime (default):** `runEmbeddedAgent(params)` (`src/agents/embedded-agent-runner/run-orchestrator.ts:71`), params type `RunEmbeddedAgentParams` (`run/params.ts:70`, ~150 fields: `sessionId, sessionKey, agentId, prompt, images, provider, model, toolsAllow, workspaceDir, timeoutMs, runId, abortSignal`, sender identity, delivery context, streaming callbacks `onPartialReply/onBlockReply/onReasoningStream/onToolResult/onAgentEvent`). The loop itself is `class Agent` in `packages/agent-core/src/agent.ts` (`prompt/continue/steer/followUp/abort/subscribe`; `AgentEvent` union; `AgentTool` with `execute` + `beforeToolCall`/`afterToolCall` policy hooks; model seam `streamFn: StreamFn` from `@openclaw/llm-core`). Tools built by `createOpenClawCodingTools` (`src/agents/agent-tools.ts:1237`), filtered by `toolsAllow` policy.
2. **`AgentHarness` plugin seam (in-process):** `src/agents/harness/types.ts:288` — `{id, label, supports(ctx), runAttempt(params: AgentHarnessAttemptParams): Promise<AgentHarnessAttemptResult>, finalizeSettledTurn?, compact?, reset?, authBinding?}`. Registry `registerAgentHarness` (`src/agents/harness/registry.ts:31`). Selected via `agentRuntime.id` config (`"openclaw"` default, `"auto"`, or plugin id). Existing: built-in (`harness/builtin-openclaw.ts:68`), `extensions/codex` (spawns Codex app-server subprocess), `extensions/copilot`. **Security caveat:** plugin harnesses run their own tools; OpenClaw can't enforce tool policy inside them — it injects deny-all system-prompt fallbacks (`selection.ts:98`).
3. **`AcpRuntime` seam (subprocess/external agents — cleanest):** `packages/acp-core/src/runtime/types.ts`:
   ```ts
   interface AcpRuntime {
     ensureSession(input: AcpRuntimeEnsureInput): Promise<AcpRuntimeHandle>;
     startTurn?(input: AcpRuntimeTurnInput): AcpRuntimeTurn;
     runTurn(input: AcpRuntimeTurnInput): AsyncIterable<AcpRuntimeEvent>;
     getCapabilities?(); getStatus?(); setMode?(); setConfigOption?();
     prepareFreshSession?(); cancel(); close(); doctor?();
   }
   ```
   - `AcpRuntimeEnsureInput`: `{sessionKey, agent, mode:"persistent"|"oneshot", resumeSessionId?, model?, modelExplicit?, thinking?, cwd?, env?}`.
   - `AcpRuntimeTurnInput`: `{handle, text, attachments?: {mediaType, data}[], mode:"prompt"|"steer", requestId, signal?}`.
   - `AcpRuntimeEvent`: `text_delta {text, stream?:"output"|"thought"}`, `status`, `tool_call {text, toolCallId?, status?, kind?}`, `done {status?, stopReason?}`, `error {message, code?, retryable?}`.
   - Registry `registerAcpRuntimeBackend({id, runtime, healthy?})` (`src/acp/runtime/registry.ts:53`); control plane `AcpSessionManager` (`src/acp/control-plane/manager.*.ts`: `resolveSession`, `initializeSession`, `runTurn({cfg, sessionKey, provenance:"human"|"agent"|"system", text, attachments?, mode, requestId, signal?, onEvent?})`, `closeSession`, backend failover). ACP session keys start with `acp:` (`isAcpSessionKey`). Config `OpenClawConfig.acp` (`src/config/types.acp.ts`): `enabled, dispatch.enabled, backend (default "acpx"), fallbacks[], defaultAgent, allowedAgents[], stream.deliveryMode`.
   - Bundled backend `extensions/acpx` (`AcpxRuntime implements AcpRuntime`, `extensions/acpx/src/runtime.ts:748`) spawns `claude-agent-acp` / `codex-acp` **subprocesses**, tracked by `AcpxProcessLeaseStore` + orphan reaper.
   - Channel path: `tryDispatchAcpReply` (`src/auto-reply/reply/dispatch-acp.ts:396`) → `acpManager.runTurn` (:747) → `createAcpReplyProjector` maps events to channel replies.
   - **Tool allowlists are NOT enforceable through ACP** — `dispatch-acp.ts:637` throws `ACP_DISPATCH_DISABLED`: *"ACP dispatch cannot enforce runtime toolsAllow for this session; use an embedded runtime for restricted tool policy."* ACP backends are fully trusted subprocesses.
   - Note: `extensions/opencode` is a **model provider** plugin, not an agent backend — don't be misled by the name.

### Config, credentials, allowlists (`src/config/`, `src/secrets/`, `src/pairing/`)

- State dir `~/.openclaw` (`src/config/paths.ts`; legacy fallback `~/.clawdbot`; overrides `OPENCLAW_STATE_DIR`/`OPENCLAW_HOME`). Config `<stateDir>/openclaw.json` (JSON5-parsed, `$include` confined to config dir unless `OPENCLAW_INCLUDE_ROOTS`). Writes `0o600`, dir `0o700` (`src/config/io.write-safety.ts`).
- Top-level schema keys (`src/config/zod-schema.root-shape.ts`): `meta, env, wizard, diagnostics, logging, update, browser, ui, secrets, auth, accessGroups, acp, models, nodeHost, agents, tools, security, bindings, broadcast, attachments, messages, tts, commands, approvals, session, cron, transcripts, hooks, channels, discovery, talk, gateway, cloudWorkers, memory, mcp, skills, plugins, surfaces, proxy`.
- **No OS keychain.** Three credential forms: (1) plaintext in `openclaw.json` (sensitive fields only redacted on display via `src/config/zod-schema.sensitive.ts` registry); (2) **SecretRef** indirection — `SecretInput = string | {source:"env"|"file"|"exec", provider, id}` (`src/config/types.secrets.ts`), provider configs under `secrets.providers` (env allowlists, file mode, exec with trustedDirs), resolution in `src/secrets/resolve.ts`, strict sites throw `UnresolvedSecretInputError`; (3) on-disk stores: `credentials/oauth.json`, `credentials/whatsapp/<accountId>/` (baileys creds, symlink-attack guard), per-agent auth-profile stores `<stateDir>/agents/<agentId>/agent/`.
- **Channel DM pairing** (`src/pairing/`): 8-char codes from `PAIRING_CODE_ALPHABET = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"` (`pairing-store.ts`); pending TTL 1 h, max 3 per account. `PairingRequest {id /* platform sender id */, code, createdAt, lastSeenAt, meta?}`; opaque request id = SHA-256 of `channel\0accountId\0id\0createdAt`. Flow: first unapproved DM → `issuePairingChallenge` (`pairing-challenge.ts:55`) replies in-chat with the code → operator `openclaw pairing approve <channel> <code>` → sender appended to the account's allowFrom. First approved sender bootstraps `commands.ownerAllowFrom` (`command-owner.ts`). **Persistence is SQLite**: `~/.openclaw/state/openclaw.sqlite` (`src/state/openclaw-state-db.paths.ts`), tables `channel_pairing_requests` + `channel_pairing_allow_entries` (`src/pairing/pairing-store-sqlite.ts`); device/node pairing also lives in that DB (`src/infra/device-pairing-store.ts`, tables `device_pairing_pending|paired`, `device_bootstrap_tokens`). **Note: `src/pairing/` is channel-sender pairing — do not conflate with node/device pairing.**

### Cron, hooks, memory (`src/cron/`, `extensions/webhooks/`, `src/memory/`, `extensions/memory-*`)

- **Cron:** `CronJob` (`src/cron/types.ts:453`) with `CronSchedule` = `{kind:"at"}|{kind:"every",everyMs}|{kind:"cron",expr,tz?}` (croner lib, IANA tz) `|{kind:"on-exit",command}|{kind:"stream",command[]}` (supervised long-lived processes whose stdout batches into prompts); payloads `systemEvent|agentTurn|command|script|heartbeat`; delivery `none|announce|webhook` with `channel/to/threadId/accountId` or `source:"last"` (session's last delivery context). Storage: SQLite table `cron_jobs` in the shared state DB. Execution `executeJobCore` (`src/cron/service/timer.ts:2482`): main-session jobs enqueue system events; isolated jobs run embedded agent turns (`runCronIsolatedAgentTurn`, lane `"cron"`, watchdog); `command` payloads exec on the gateway host. Webhook delivery by the gateway (`postCronWebhook`, `src/gateway/server-cron-notifications.ts:200`) with Bearer `cron.webhookToken` + SSRF guard. **Cron jobs default to `toolsAllow: ["*"]`** (`applyDefaultCronToolsAllow`).
- **Hooks:** `/hooks/wake` and `/hooks/agent` (`src/gateway/server/hooks-request-handler.ts`) — Bearer `hooks.token` only (query-param tokens explicitly rejected), rate-limited; `/hooks/agent` synthesizes an ephemeral isolated cron job. `hooks.allowRequestSessionKey` + `hooks.allowedSessionKeyPrefixes` let a token holder pick the target session. Gmail: supervised `gog gmail watch serve` sidecar POSTing to `/hooks/gmail` (`src/hooks/gmail*.ts`).
- **Webhooks plugin** (`extensions/webhooks/`): per-route `sessionKey` + `secret` (SecretInput), default path `/plugins/webhooks/<routeId>`, 256 KB body cap, constant-time compare, drives TaskFlows (not cron).
- **Memory:** `memory-core` (bundled) exposes agent tools `memory_search {query, maxResults?, minScore?, corpus?}` / `memory_get {path, from?, lines?, corpus?}` over `MEMORY.md` + `memory/*.md`; index = `node:sqlite` FTS5 + optional sqlite-vec (`MemoryIndexManager`, `src/memory/manager.ts:299`), embeddings via configurable provider (default openai; `local` = node-llama-cpp GGUF). Trust note (`tools.ts:485`): *"The trusted runtime chooses the recall corpus; model-authored arguments cannot broaden it."* `memory-lancedb` (separate package): `memory_recall/memory_store/memory_forget` over LanceDB with per-agent scoping. `active-memory`: blocking memory sub-agent on `before_prompt_build`. "Dreaming" consolidation promotes snippets into `MEMORY.md`.

### Install / upgrade / packaging (`openclaw.mjs`, `src/daemon/`, `src/infra/update-*.ts`)

- Bin `openclaw.mjs` = launcher (Node version gate, compile-cache respawn, fast paths for `--version`/help) → `dist/entry.js`. Build: **tsdown** (rolldown) via `tsdown.config.ts` (+`tsdown.ai.config.ts`), `pnpm build` → `scripts/build-all.mjs`. npm tarball ships `dist/`, `docs/`, `skills/`, `npm-shrinkwrap.json` (pinned transitive deps) and **excludes ~70 bundled extensions** (those ship separately via npm/ClawHub). `preinstall`/`postinstall` scripts run on every `npm i -g` (postinstall prunes dist and hotfixes bundled baileys by string-matching `node_modules` — supply-chain surface).
- Daemon: launchd user agent `ai.openclaw.gateway` (secrets in separate 0o600 env file, plist 0o644), systemd **user** units (`Restart=always`, `RestartPreventExitStatus=78`, CR/LF injection guards), Windows schtasks + Startup-folder fallback. Externally supervised mode: `OPENCLAW_SUPERVISOR_MODE=external` blocks service mutation **and** self-update — the escape hatch for embedding.
- Self-update: `openclaw update --channel stable|extended-stable|beta|dev` re-installs from npm via detected manager (npm/pnpm/bun) then runs the **newly installed** `openclaw doctor`; git checkouts do fetch/rebase/pnpm install/build. Channels map to npm dist-tags (`stable→latest`); tags `vYYYY.M.D[-beta.N]` (`openclaw-npm-release.yml` regex, OIDC trusted publishing). Gateway RPC `update.run` requires `operator.admin`. `update.auto.enabled` defaults **false**.
- Native deps: core has no node-gyp at install; native-ish: `@lydell/node-pty` (prebuilt), optional `sqlite-vec`, WASM (`quickjs-wasi`, tree-sitter). Matrix extension's crypto addon downloads a prebuilt `.node`.

## 3. Seams for an "OpenClawAdapter"

Ordered by how much of OpenClaw each reuses. The guiding fact: OpenClaw's plugin SDK is a set of facades (`packages/plugin-sdk/src/*` re-export `src/plugin-sdk/*` and `src/channels/plugins/*`), published as `openclaw/plugin-sdk/*` subpaths **version-locked to the host** (all packages `2026.7.2`) — there is no independent semver stability for any of these contracts.

### 3.1 Agent seam (where the OmniHarness daemon plugs in) — `AcpRuntime`

Implement `AcpRuntime` (`packages/acp-core/src/runtime/types.ts`) and register with `registerAcpRuntimeBackend({id:"omniharness", runtime})` (`src/acp/runtime/registry.ts:53`). Minimum viable: `ensureSession` + `runTurn` streaming `text_delta` events and terminating with `done`. The daemon receives `{sessionKey, agent, mode, model?, cwd?}` at session setup and `{text, attachments?: {mediaType, data}[], mode:"prompt"|"steer", requestId, signal}` per turn. Route conversations to it via `agents.bindings[]` entries `{type:"acp", agentId, match, acp:{backend:"omniharness", ...}}` and `acp.allowedAgents`. This is exactly how `extensions/acpx` wraps claude/codex ACP subprocesses — copy its shape (`extensions/acpx/src/runtime.ts:748`, process leasing in `src/process-lease.ts`).
- If ACP-over-stdio is a better transport for the daemon, mirror `extensions/acpx`'s use of `acpx@0.11.2` and let the daemon speak the Agent Client Protocol; OpenClaw already ships the client side.
- **Consequence:** tool policy, sandboxing, and approvals must live inside the OmniHarness daemon — OpenClaw explicitly refuses restrictive `toolsAllow` on ACP sessions (`dispatch-acp.ts:637`).

### 3.2 Gateway connection seam (OmniHarness as an operator client)

Use `packages/gateway-client` (`src/client.ts` — reference implementation) as the protocol contract:
1. WS connect to `ws://<host>:18789`, await `connect.challenge` event.
2. Send `req "connect"` with `ConnectParams`: closed-enum `client.id`/`client.mode` (see `packages/gateway-protocol/src/client-info.ts`), `minProtocol:4, maxProtocol:4`, `auth:{token}` and ideally `device:{id, publicKey, signature, signedAt, nonce}` (Ed25519; sign `buildDeviceAuthPayloadV3` including the token and challenge nonce).
3. On `hello-ok`, persist `auth.deviceToken` and reconnect with it thereafter.
4. Subscribe: `sessions.subscribe` / `sessions.messages.subscribe`; consume scoped `EventFrame`s (`chat`, `agent`, `session.message`, `exec.approval.requested`, …) with per-client `seq` gap handling.
- Frame schemas to vendor: `packages/gateway-protocol/src/schema/frames.ts`, `schema/nodes.ts`, `schema/error-codes.ts`, `version.ts`. Pin to `PROTOCOL_VERSION = 4`.
- For a co-located (loopback) adapter, the documented "trusted same-process backend client" path (`client.id:"gateway-client"`, `mode:"backend"`) may omit the device proof on loopback+shared-secret — convenient but inherits full local trust; prefer real device identity for anything off-loopback.

### 3.3 Channel message seam

Two options:
- **Reuse OpenClaw channels as-is (wrap):** run the OpenClaw gateway, let `extensions/telegram|slack|discord|whatsapp|…` normalize inbound into `MsgContext`, and take over only below `runPreparedReply` — i.e. bind the target sessions to the ACP backend (3.1). You inherit dmPolicy/pairing, media staging (`saveMediaBuffer`, `src/media/store.ts:482`), chunking, typing, and durable delivery for free.
- **Implement a new channel (wrap the interface):** implement `ChannelPlugin` (`src/channels/plugins/types.plugin.ts:60`) — realistically via `createChatChannelPlugin` (`src/plugin-sdk/core.ts:822`) — providing: `config: ChannelConfigAdapter` (account listing/resolution, `resolveAllowFrom`), `gateway: ChannelGatewayAdapter.startAccount` (your receive loop; build `MsgContext`, call the host-injected `channelRuntime.reply.dispatchReplyWithBufferedBlockDispatcher` with a `deliver` callback), `outbound: ChannelOutboundAdapter` (`sendText/sendMedia/sendPayload` → `OutboundDeliveryResult`), `security.resolveDmPolicy`, `pairing` (via `createChannelPairingChallengeIssuer`, `src/plugin-sdk/channel-pairing.ts:30`). Declare `openclaw.channel` + `openclaw.plugin.json` in the package. Package as an external plugin (`defineChannelPluginEntry`).
- Outbound contract to honor: `ReplyPayload` (`src/auto-reply/reply-payload.ts`) in, `OutboundDeliveryResult` / `MessageReceipt` out; text chunk limits per channel; media must go through `loadOutboundMediaFromUrl` policy (`src/plugin-sdk/outbound-media.ts:32`: maxBytes, local roots, SSRF guard).

### 3.4 Node pairing seam (phones/remote devices as OmniHarness peripherals)

Two sub-options:
- **Node client side (make OmniHarness-controlled devices pair as nodes):** implement the node half of the protocol — connect with `role:"node"`, declare `caps`/`commands`, answer `node.invoke.request` events with `node.invoke.result` reqs. Reference: `src/node-host/runner.ts` (`runNodeHost`), `src/node-host/invoke.ts` (`handleInvoke`, `sendInvokeResult` :1029), mobile impls `apps/ios/Sources/Gateway/GatewayConnectionController+Capabilities.swift`, `apps/android/.../GatewayProtocol.kt`. Expect operator approval (`openclaw devices approve` + `openclaw nodes approve`) unless bootstrap-token onboarding (`device.pair.setupCode`) is used.
- **Gateway side (reuse pairing UX):** the `/pair` command + QR/setup-code flow in `extensions/device-pair/` and the approval stores in `src/infra/device-pairing.ts` / `node-pairing.ts` are all reachable via RPCs (`device.pair.*`, `node.pair.*`, scope `operator.pairing`) — an adapter can drive pairing entirely through the gateway client without touching the DB.

### 3.5 Remote approvals seam

Approval events are already on the event bus: `exec.approval.requested|resolved`, `session.approval`, `device.pair.requested`, `node.pair.requested` (scope-gated to `operator.approvals`/`operator.pairing`). An adapter with those scopes subscribes to the events and resolves via the corresponding RPCs (`exec.approvals.*`, `device.pair.approve`, `node.pair.approve`). For approvals that must reach a phone, the existing path is node push (APNs wake, `maybeWakeNodeWithApns`) — or deliver to any chat channel via the `message` tool / `chat.send`-equivalent outbound (`routeReply`, `src/auto-reply/reply/route-reply.ts`).

### 3.6 Media seam

Inbound: channels download → `saveMediaBuffer(buffer, contentType, "inbound", maxBytes)` (`src/media/store.ts:482`) → paths on `MsgContext.MediaPaths[]` + `media: MediaFact[]` (position = attachment identity). ACP turns carry attachments as base64 `{mediaType, data}` (`AcpRuntimeTurnInput.attachments`) — that's the format the OmniHarness daemon will receive. Outbound: set `ReplyPayload.mediaUrl(s)`; the channel's `sendMedia` enforces `OutboundMediaAccess` (maxBytes, local roots, SSRF). For hosted-URL channels use `createHostedOutboundMediaStore` (tokenized expiring URLs, `src/plugin-sdk/outbound-media.ts`).

## 4. Security observations

- **Auth model in one sentence:** the gateway shared secret (token/password) gets you a connection; real authority comes from paired-device records (Ed25519 proof + per-role, per-scope device tokens); channel-level authorization is sender-ID allowlists; everything else (session keys, sender names) is routing data.
- **Normative trust statements** (SECURITY.md): "anyone with Gateway auth = trusted operator"; "the model/agent is **not** a trusted principal"; "Session identifiers (`sessionKey`, session IDs, labels) are routing controls, **not** per-user authorization boundaries"; anyone who can write `~/.openclaw` or the process env is trusted; plugins run in-process at full privilege; sandbox is **off by default**.
- **What must NOT be trusted:**
  - **Session keys / sessionIds as auth.** They are parseable, forgeable routing strings. Worse, `hooks.allowRequestSessionKey` + `hooks.allowedSessionKeyPrefixes` let any holder of the hooks Bearer token choose which session a message lands in (`src/gateway/server/hooks-request-handler.ts:280-301`).
  - **Sender IDs beyond the allowlist gate.** allowFrom matching is the *only* channel authz, and it keys off platform sender IDs; mutable identifiers (usernames/emails/nicks) are explicitly flagged `dangerous` and disabled unless `mutableIdentifierMatching === "enabled"` (`src/channels/message-access/allowlist.ts`). Owner status is likewise sender-ID-derived (`commands.ownerAllowFrom`, `tools.elevated.allowFrom`, `tools.toolsBySender`).
  - **Channel-provided metadata in prompts.** Group subjects, sender names etc. are fenced as untrusted (`buildUntrustedChannelMetadata`, `src/security/channel-metadata.ts`; `wrapExternalContent`, `src/security/external-content.ts` with homoglyph/zero-width marker sanitization and ChatML special-token stripping). Our adapter must preserve this fencing when constructing prompts.
  - **Trusted-context fields must be server-set, never sender-set:** `CommandAuthorized`, `InboundAccessAuthorized`, `OwnerAllowFrom`, `GatewayClientScopes` on `MsgContext`; `requesterSenderId`/`senderIsOwner` on `ChannelMessageActionContext` ("must never be sourced from tool/model-controlled params", `types.core.ts:664`).
  - **The loopback "backend" client path** skips device proof — full trust by design; never expose it off-loopback.
  - **`gateway.auth.mode:"none"` exists.** So does `dmPolicy:"open"` (requires `"*"` in allowFrom). `openclaw doctor` flags these; an adapter should too.
- **Fail-closed machinery worth reusing:** ingress resolver reason codes (`src/channels/message-access/runtime.ts`), default-deny `CommandAuthorized` (`finalizeInboundContext`), fail-closed event scope guards (`EVENT_SCOPE_GUARDS`), pre-auth hardening (64 KiB cap, one `connect` method, per-IP pre-auth budget, handshake timeout), constant-time secret compares, auth rate limiting, SSRF guard on outbound webhooks/media (`fetchWithSsrFGuard`), CR/LF injection guards in unit-file rendering.
- **Credential exposure:** channel tokens, `gateway.auth.token`, `hooks.token` are frequently **plaintext** in `openclaw.json` (0o600) unless the operator wires SecretRefs; WhatsApp/baileys creds and OAuth tokens are plaintext files under `~/.openclaw/credentials/`. Treat the state dir as a secret root.
- **ACP = full trust.** An ACP backend runs its own tools with its own permissions; OpenClaw can't enforce `toolsAllow` across it (by design, `dispatch-acp.ts:637`). The OmniHarness daemon must own its own sandbox/approval story.
- **Update path = code execution.** Self-update runs `npm/pnpm/bun install -g <spec>` then executes the new tree; `--tag main` tracks a moving GitHub branch. `update.run` is `operator.admin`-gated. Use `OPENCLAW_SUPERVISOR_MODE=external` when embedding.

## 5. Reimplement vs. wrap; protocol compatibility

**Wrap (depend on OpenClaw packages / run its gateway):**
- Gateway server + protocol (`src/gateway/*`, `packages/gateway-protocol`, `packages/gateway-client`) — the handshake/device-proof/pairing machinery is deep and security-critical; reimplementing invites subtle auth bugs. Client side can be a thin vendored implementation of the four frame types + `connect` handshake (all TypeBox schemas in `packages/gateway-protocol/src/schema/`).
- Channel plugins we want to support (telegram/slack/discord/whatsapp/signal) — running them as plugins inside an OpenClaw gateway is dramatically cheaper than porting grammy/bolt/baileys integrations.
- DM pairing + allowlist store (`src/pairing/`), ingress resolver, media staging/policy, reply dispatcher lanes, typing, cron delivery to channels.

**Reimplement (own code, OpenClaw-compatible):**
- The agent runtime — that is OmniHarness itself, behind the `AcpRuntime` interface (§3.1).
- Any adapter process supervising the gateway (health, restart, config templating) — set `OPENCLAW_SUPERVISOR_MODE=external` so OpenClaw's daemon/update machinery stays out of the way.
- Config generation: write `openclaw.json` ourselves (JSON5, 0o600), using SecretRefs for all tokens from day one.

**Compatibility notes:**
- **Version pinning is mandatory.** Protocol is at `PROTOCOL_VERSION = 4` (`MIN_NODE_PROTOCOL_VERSION = 3`); schemas are TypeBox and evolve per release; the plugin SDK has no independent semver. Pin an exact OpenClaw version per OmniHarness release and vendor `packages/gateway-protocol` schemas for any reimplemented client.
- **String formats are load-bearing.** Session keys (`agent:<id>:<channel>:<accountId>:direct:<peerId>`, `:thread:` suffixes, `acp:`/`cron:`/`subagent:` prefixes), case-preservation spans (Signal/Matrix), `dmScope` variants — sandbox scoping, reset, routing, and cross-channel delivery all parse these. Reproduce exactly or delegate to `src/routing/session-key.ts` helpers.
- `MsgContext` is a ~200-field grab bag; the fields that matter for an agent seam are the Body/From/To/SessionKey/sender/media/trust-marker subset — but `finalizeInboundContext` must remain the only place `CommandAuthorized` becomes true.
- **Don't conflate the two pairing systems**: `src/pairing/` = channel sender allowlists; `src/infra/device-pairing.ts` + `node-pairing.ts` = device/node identity. Shared naming, different stores (both in the same SQLite DB), different RPC families.
- WebChat is internal-only (no dmPolicy, not an outbound target); external WebChat-like surfaces should be built as gateway operator clients, not channels.
- License: MIT — wrapping/vendoring is fine; keep `THIRD_PARTY_NOTICES.md`'s pi-mono attribution, and note OpenClaw bundles deps (zod etc.) without per-dep notices if we redistribute its dist output.

## 6. Risks

1. **Moving-target coupling (high).** Calendar-versioned releases (`2026.7.2` at audit time), protocol v4, version-locked plugin SDK, ~28k-file monorepo with extremely active development (commit #112939). Any wrap strategy needs a pinned version + a repeatable upgrade/audit loop.
2. **Security posture mismatch (high).** OpenClaw is a single-user, host-first personal assistant: sandbox off by default, plaintext secrets by default, operator = owner. OmniHarness must impose its own multi-tenant/tool-policy story at the ACP seam and never inherit OpenClaw's ambient trust (loopback backend clients, `mode:"none"`, `dmPolicy:"open"`).
3. **ACP trust gap (medium-high).** No tool-policy enforcement across the ACP boundary is *by design*; if our threat model needs mediated tools, we must mediate inside the daemon or fall back to the in-process `AgentHarness` seam (much larger contract: `AgentHarnessAttemptParams` ≈ full `EmbeddedRunAttemptParams`).
4. **In-process plugin model (medium).** Channel plugins run in the gateway process at full privilege; a buggy baileys/grammy dependency is inside our trust boundary. Mitigate by running the OpenClaw gateway as its own supervised process with least OS privilege.
5. **SQLite state coupling (medium).** Pairing/allowlist/cron/device state lives in `~/.openclaw/state/openclaw.sqlite` with schema version `state: 5`. Touch it only through RPCs; direct DB access will break on migrations.
6. **Supply chain (medium).** `npm i -g openclaw` runs pre/postinstall scripts (which patch `node_modules` by string matching); Matrix crypto downloads prebuilt binaries; self-update executes freshly installed code. Pin and verify tarballs; disable auto-update (default off — keep it off).
7. **Session-key spoof surface (medium-low).** Anything in our stack that treats session keys or hook-chosen session targets as security boundaries re-introduces a bug class OpenClaw explicitly disclaims.
8. **Notice/attribution gap (low).** Bundling OpenClaw dist inherits its minimal third-party notices; run our own license scan if we redistribute.
9. **Feature gravity (low).** Cron jobs default to `toolsAllow:["*"]` and delivery falls back to a session's *last* route (`source:"last"`) — convenient but leaky defaults to override in generated configs.
